package com.example.web.quiz.services;

import com.example.web.quiz.entities.reponse;

import java.util.List;

public interface reponseInterface {

    void saveResponse(reponse response);

    //List<reponse> getResponsesByUser(User user);
}
